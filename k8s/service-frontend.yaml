apiVersion: apps/v1
kind: Deployment
metadata:
  name: projeto-sd-frontend-deployment
  namespace: sge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: projeto-sd-frontend
  template:
    metadata:
      labels:
        app: projeto-sd-frontend
    spec:
      containers:
        - name: projeto-sd-frontend
          image: miguelfcosta/login-frontend:latest  
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: login-frontend-service
  namespace: sge
spec:
  selector:
    app: projeto-sd-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000 
  type: ClusterIP